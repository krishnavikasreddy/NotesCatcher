// config for notes styling

const defaultNoteStyle = {
  background: 'green',
  color: 'white',
};

const defaultFollowerStyle = {
  position: 'absolute',
  top: '0px',
  left: '0px',
  color: 'red',
  background: 'black',
  visibility: 'hidden',
};

export {
  defaultNoteStyle,
  defaultFollowerStyle,
};
